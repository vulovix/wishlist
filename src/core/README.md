In this folder we should create utilities for packages we offer.

We don't want to couple our application logic to the packages directly, instead we want to have this layer between our App and Package.